<h1>Listing in_home_safe_or_vaults</h1>

<table>
  <tr>
    <th>Possession</th>
    <th>Name</th>
    <th>Location</th>
    <th>Code</th>
    <th>Photo</th>
    <th>Note</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @in_home_safe_or_vaults.each do |in_home_safe_or_vault| %>
  <tr>
    <td><%= in_home_safe_or_vault.possession_id %></td>
    <td><%= in_home_safe_or_vault.name %></td>
    <td><%= in_home_safe_or_vault.location %></td>
    <td><%= in_home_safe_or_vault.code %></td>
    <td><%= in_home_safe_or_vault.photo %></td>
    <td><%= in_home_safe_or_vault.note %></td>
    <td><%= link_to 'Show', in_home_safe_or_vault %></td>
    <td><%= link_to 'Edit', edit_in_home_safe_or_vault_path(in_home_safe_or_vault) %></td>
    <td><%= link_to 'Destroy', in_home_safe_or_vault, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New In home safe or vault', new_in_home_safe_or_vault_path %>
